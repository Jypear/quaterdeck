{
  "name": "quaterdeck-vendor",
  "private": true,
  "description": "Pins Bootstrap/HTMX/Alpine/Fraunces versions and copies their dist builds into static/vendor/ so the app serves them locally with no CDN.",
  "scripts": {
    "vendor": "node scripts/vendor-assets.js"
  },
  "dependencies": {
    "@fontsource/fraunces": "^5.2.9",
    "alpinejs": "^3.14.9",
    "bootstrap": "^5.3.8",
    "echarts": "^5.6.0",
    "htmx.org": "^2.0.7"
  }
}
